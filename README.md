## fs Module | NodeJS

[Node.js](https://nodejs.org/) includes `fs` module to access physical `file system`. The `fs` module is responsible for all the `asynchronous` or `synchronous` file I/O operations.

### Online Text Editor 
Write your [Node.js](https://nodejs.org/) with [Online NodeJS Editor](https://repl.it/languages/nodejs) which is very suitable for Developers

